[theme]
# Solo el color corporativo; fondo y texto se adaptan automáticamente al modo del sistema
primaryColor = "#ED7D31"
font = "sans serif"

[server]
headless = true
runOnSave = true
fileWatcherType = "poll"

[browser]
gatherUsageStats = false
